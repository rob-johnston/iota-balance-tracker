import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.iota.trackerapp',
  appName: 'iota balance tracker',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
