import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.iota.mobile',
  appName: 'iota-balance-app',
  webDir: 'public',
  bundledWebRuntime: true,
};

export default config;
